import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    color: 'white',
    letterSpacing: 2,
    fontSize: 20,
  },
  touchable: {
    borderWidth: 1,
    borderColor: 'white',
    padding: 10,
    borderRadius: 5,
    margin: 20,
  },
});
