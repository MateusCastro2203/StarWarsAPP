import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  fullScreen: {
    backgroundColor: '#272B30',
    height: '100%',
    alignItems: 'center',
  },
  title: {
    fontSize: 50,
    fontWeight: 'bold',
    color: '#FFE300',
  },
});
